interface UserInfo {
	pixels: number
	maxPixels: number
	lastTicked: number
	placed: number
}

class User {
	private _pixels: number = 100;
	private _maxPixels: number = 100;
	private _placed: number = 0;
}
